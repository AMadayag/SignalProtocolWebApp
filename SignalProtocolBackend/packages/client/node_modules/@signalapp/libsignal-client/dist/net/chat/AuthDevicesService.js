//
// Copyright 2026 Signal Messenger, LLC.
// SPDX-License-Identifier: AGPL-3.0-only
//
import { AuthenticatedChatConnection } from '../Chat.js';
import * as NativeNice from '../../NativeNice.js';
AuthenticatedChatConnection.prototype.setDeviceName = async function ({ deviceId, encryptedName, }, options) {
    return await NativeNice.AuthenticatedChatConnection_set_device_name({
        asyncContext: this.asyncContext,
        abortSignal: options?.abortSignal,
        chat: this.chatService,
        deviceId,
        encryptedName,
    });
};
AuthenticatedChatConnection.prototype.removeDevice = async function ({ deviceId, }, options) {
    return await NativeNice.AuthenticatedChatConnection_remove_device({
        asyncContext: this.asyncContext,
        abortSignal: options?.abortSignal,
        chat: this.chatService,
        deviceId,
    });
};
AuthenticatedChatConnection.prototype.getDevices = async function (options) {
    return await NativeNice.AuthenticatedChatConnection_get_devices({
        asyncContext: this.asyncContext,
        abortSignal: options?.abortSignal,
        chat: this.chatService,
    });
};
AuthenticatedChatConnection.prototype.clearPushToken = async function (options) {
    return await NativeNice.AuthenticatedChatConnection_clear_push_token({
        asyncContext: this.asyncContext,
        abortSignal: options?.abortSignal,
        chat: this.chatService,
    });
};
//# sourceMappingURL=AuthDevicesService.js.map